# will inject 'application' namespace in bootstrap phase
apollo.bootstrap.enabled=true
# will inject 'application', 'FX.apollo' and 'application.yml' namespaces in bootstrap phase
apollo.bootstrap.namespaces=application,TEST1.ww-public

#测试server.port和server.servlet.context-path可以直接配置在apollo服务端，
#项目启动时可以直接从apollo端读取并进行设置。其他属性待测
#server.port=8180
#server.servlet.context-path=/apollo-study
spring.application.name=apollo-study

logging.level.com.apollo.demo.logger=warn


